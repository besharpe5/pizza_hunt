const { Schema, model } = require('mongoose');

const PizzaSchema = new Schema({
    pizzaName: {
        type: String
    },
    createdBy: {
        type: String
    },
    createdAt: {
        type: Date,
        default: Date.now
    },
    size: {
        type: String,
        default: 'Large'
    },
    toppings: []
});

// this creates the Pizza model using the PizzSchema
const Pizza = model('Pizza', PizzaSchema);

// this exports the Pizza model
module.exports = Pizza;